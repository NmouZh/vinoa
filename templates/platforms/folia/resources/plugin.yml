# Generated by vinoa. Bukkit/Spigot metadata for Folia; Paper reads this file too.
name: {{ pluginName }}
version: "${version}"
main: {{ mainClass }}
# Folia needs this flag before it will load a Bukkit plugin.
folia-supported: true
{% if cap_api_version_line %}
api-version: '{{ pluginYmlApiVersion }}'
{% endif %}
description: '{{ pluginDescription }}'
{% if has_authors %}
authors:
{% for a in authors %}
  - '{{ a }}'
{% endfor %}
{% endif %}
commands:
  {{ commandName }}:
    description: '{{ pluginDescription }}'
    usage: /{{ commandName }} <message>
{% if cap_permissions %}
    permission: {{ permissionNode }}.example
    permission-message: 'You do not have permission to use this command.'
{% endif %}
{% if cap_permissions %}
permissions:
  {{ permissionNode }}.example:
    description: 'Allows using /{{ commandName }}.'
    default: op
  {{ pluginId }}.admin:
    description: 'Administrative access to {{ pluginName }}.'
    default: op
    children:
      {{ permissionNode }}.example: true
{% endif %}
