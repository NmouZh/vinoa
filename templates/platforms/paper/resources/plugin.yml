# Generated by vinoa. Bukkit/Spigot metadata; Paper reads this file too.
name: {{ pluginName }}
# Quoted on purpose: an unquoted version is coerced by SnakeYAML (for example 1.10 -> 1.1).
version: "${version}"
main: {{ mainClass }}
{% if cap_api_version_line %}
api-version: '{{ pluginYmlApiVersion }}'
{% endif %}
description: '{{ pluginDescription }}'
{% if has_authors %}
authors:
{% for a in authors %}
  - '{{ a }}'
{% endfor %}
{% endif %}
{% if cap_placeholderapi %}
softdepend:
  - PlaceholderAPI
{% endif %}
{% if cap_libraries and cap_bstats or cap_libraries and cap_sqlite or cap_libraries and cap_placeholderapi %}
# Downloaded by the server and added to the plugin classpath (Minecraft 1.16.5 and newer).
libraries:
{% if cap_bstats %}
  - {{ thirdparty.bstats }}
{% endif %}
{% if cap_sqlite %}
  - {{ thirdparty.sqlite_jdbc }}
{% endif %}
{% if cap_placeholderapi %}
  - {{ thirdparty.placeholderapi }}
{% endif %}
{% endif %}
commands:
  {{ commandName }}:
    description: '{{ pluginDescription }}'
    usage: /{{ commandName }} <message>
{% if cap_permissions %}
    permission: {{ permissionNode }}.example
    permission-message: 'You do not have permission to use this command.'
{% endif %}
{% if cap_permissions %}
permissions:
  {{ permissionNode }}.example:
    description: 'Allows using /{{ commandName }}.'
    default: op
  {{ pluginId }}.admin:
    description: 'Administrative access to {{ pluginName }}.'
    default: op
    children:
      {{ permissionNode }}.example: true
{% endif %}
