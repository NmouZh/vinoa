{% if is_lang_en %}
# {{ pluginName }}

{{ pluginDescription }}

## Requirements

- Minecraft: `{{ mcVersion }}`
{% for p in platforms %}
- `platforms:{{ p }}`: Java `{{ javaTargets[p] }}`
{% endfor %}
- Gradle: use the wrapper in this repository (Gradle `{{ gradleVersion }}`)

## Modules

| Module | Purpose |
|---|---|
| `core` | Platform-independent logic. It must not import any server API. |
{% for p in platforms %}
| `platforms:{{ p }}` | `{{ p }}` adapter: entry point, commands, listeners, resources. |
{% endfor %}

## Build

```
./gradlew build
```

Build output: `<module>/build/libs/<module>-0.1.0-SNAPSHOT.jar` (the `core` module also produces
a plain library jar).

{% if cap_run_paper %}
## Local test server

```
./gradlew runServer
```

The run task downloads the matching Paper build for `{{ mcVersion }}`.
{% else %}
## Local test server

There is no run task for this target. Build a server jar yourself (for example with Spigot
BuildTools or the vanilla server for `{{ mcVersion }}`) and drop the built plugin jar into its
`plugins/` directory.
{% endif %}
## Command and permissions

| Command | Permission | Description |
|---|---|---|
| `/{{ commandName }} <message>` | `{{ permissionNode }}.example` | Broadcasts `<message>` to every online player. |
{% if cap_permissions %}
| — | `{{ pluginId }}.admin` | Administrative umbrella node; it inherits `{{ permissionNode }}.example`. |
{% endif %}
## Configuration

`config.yml`:

| Key | Meaning |
|---|---|
| `welcome-message` | Message shown to a player who joins. `{player}` is replaced by the player name. |
| `broadcast-prefix` | Prefix prepended to broadcast messages. |
| `debug` | Enables verbose logging. |

## License

{{ license }}. See `LICENSE`.
{% if cap_dual_lang %}
A Chinese version of this README is available in [`README.md`](README.md).
{% endif %}
This project was generated by vinoa.

## References

The runtime behaviour encoded here follows the upstream documentation:
[Paper plugin metadata](https://docs.papermc.io/paper/dev/plugin-yml/),
[Paper plugin format](https://docs.papermc.io/paper/dev/getting-started/paper-plugins/),
[Paper project setup](https://docs.papermc.io/paper/dev/project-setup/),
[Paper events](https://docs.papermc.io/paper/dev/api/event-api/event-listeners/),
[Spigot/Bukkit legacy API](https://hub.spigotmc.org/javadocs/spigot/).
{% else %}
# {{ pluginName }}

{{ pluginDescription }}

## 环境要求

- Minecraft：`{{ mcVersion }}`
{% for p in platforms %}
- `platforms:{{ p }}`：Java `{{ javaTargets[p] }}`
{% endfor %}
- Gradle：使用仓库内的 wrapper（Gradle `{{ gradleVersion }}`）

## 模块

| 模块 | 用途 |
|---|---|
| `core` | 平台无关逻辑，禁止 import 任何服务端 API。 |
{% for p in platforms %}
| `platforms:{{ p }}` | `{{ p }}` 适配层：入口、命令、监听器、资源。 |
{% endfor %}

## 构建

```
./gradlew build
```

产物路径：`<模块>/build/libs/<模块>-0.1.0-SNAPSHOT.jar`（`core` 模块另外产出一个普通库 jar）。

{% if cap_run_paper %}
## 本地测试服

```
./gradlew runServer
```

该任务会自动下载与 `{{ mcVersion }}` 匹配的 Paper 构建。
{% else %}
## 本地测试服

该目标没有内置 run 任务。请自行准备服务端 jar（例如用 Spigot BuildTools 或 `{{ mcVersion }}`
的原版服务端），把构建出的插件 jar 放进它的 `plugins/` 目录。
{% endif %}
## 命令与权限

| 命令 | 权限 | 说明 |
|---|---|---|
| `/{{ commandName }} <message>` | `{{ permissionNode }}.example` | 把 `<message>` 广播给所有在线玩家。 |
{% if cap_permissions %}
| — | `{{ pluginId }}.admin` | 管理总节点，继承 `{{ permissionNode }}.example`。 |
{% endif %}
## 配置

`config.yml`：

| 键 | 含义 |
|---|---|
| `welcome-message` | 玩家加入时显示的消息；`{player}` 会替换成玩家名。 |
| `broadcast-prefix` | 广播消息的前缀。 |
| `debug` | 打开详细日志。 |

## 许可证

{{ license }}，见 `LICENSE`。
{% if cap_dual_lang %}
英文版说明见 [`README.en.md`](README.en.md)。
{% endif %}
本工程由 vinoa 生成。

## 参考来源

本模板遵循的上游行为来自官方文档：
[Paper plugin.yml](https://docs.papermc.io/paper/dev/plugin-yml/)、
[Paper 插件格式](https://docs.papermc.io/paper/dev/getting-started/paper-plugins/)、
[Paper 项目搭建](https://docs.papermc.io/paper/dev/project-setup/)、
[Paper 事件](https://docs.papermc.io/paper/dev/api/event-api/event-listeners/)、
[Spigot/Bukkit 传统 API](https://hub.spigotmc.org/javadocs/spigot/)。
{% endif %}
