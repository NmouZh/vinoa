# Gradle daemon and build-cache settings for the generated project.
# No version numbers live here: gradle/libs.versions.toml is the single source of truth.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
